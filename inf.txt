Линейные алгоритмы

1)
#Вводим длину ребра куба
a = float(input("Введите длину ребра куба: "))
#Вычисляем объем куба
v = a**3
#Вычисляем площадь боковой поверхности куба
kub = 4 * a**2
#Выводим результат
print('Объем куба:', v)
print('Площадь боковой поверхности куба:', kub)

2)
#Вводим длину окружности
C = float(input("Введите длину окружности: "))
#Вычисляем радиус круга
R = C / (2 * 3.14)
#Вычисляем площадь круга
S = (C**2) / (4 * 3.14)
#Выводим результаты
print('Радиус круга:', R)
print('Площадь круга:', S)

3)
import math

# Вводим длины сторон прямоугольника
a = float(input("Введите длину стороны a прямоугольника: "))
b = float(input("Введите длину стороны b прямоугольника: "))
# Вычисляем периметр прямоугольника
P = 2 * a + 2 * b
# Вычисляем площадь прямоугольника
S = a * b
# Вычисляем длину диагонали прямоугольника
d = math.sqrt(a**2 + b**2)
# Выводим результаты
print('Периметр прямоугольника:', P)
print('Площадь прямоугольника:', S)
print('Длина диагонали прямоугольника:', d)


Разветвляющиеся алгоритмы

1) 
x = float(input("Введите длину стороны x: "))
y = float(input("Введите длину стороны y: "))
z = float(input("Введите длину стороны z: "))
#Проверяем, существует ли треугольник
if x + y > z and x + z > y and y + z > x:
    print("Треугольник существует")
else:
    print("Треугольник не существует")

2) 
#Вводим координаты точки М1
x1 = float(input("Введите координату x1: "))
y1 = float(input("Введите координату y1: "))

#Вводим координаты точки М2
x2 = float(input("Введите координату x2: "))
y2 = float(input("Введите координату y2: "))

#Вычисляем расстояния от каждой точки до начала координат
l1 = (x1**2 + y1**2)**0.5
l2 = (x2**2 + y2**2)**0.5

#Определяем, какая точка ближе и выводим её координаты
if l1 < l2:
    print(f"Точка M1({x1}, {y1}) ближе к началу координат")
else:
    print(f"Точка M2({x2}, {y2}) ближе к началу координат")

3)
import math

#Вводим значения стороны квадрата и радиуса круга
a = float(input("Введите сторону квадрата (a): "))
r = float(input("Введите радиус круга (r): "))

#Вычисляем площади фигур
square_area = a**2
circle_area = math.pi * r**2

#Определяем, какая фигура имеет большую площадь и выводим результат
if square_area > circle_area:
    print('Квадрат имеет большую площадь:', square_area)
else:
    print('Круг имеет большую площадь:', circle_area)


4)
import math

# Вводим координаты точки M
x = float(input("Введите координату x точки M: "))
y = float(input("Введите координату y точки M: "))

# Вводим координаты центра круга и его радиус
x0 = float(input("Введите координату x0 центра круга: "))
y0 = float(input("Введите координату y0 центра круга: "))
r = float(input("Введите радиус круга: "))

# Вычисляем расстояние от точки M до центра круга
l = math.sqrt((x - x0)**2 + (y - y0)**2)

# Проверяем, попадает ли точка в круг
if l <= r:
    print("Точка M попадает в круг")
else:
    print("Точка M не попадает в круг")


циклические алгоритмы
1) 
import math

#Вводим натуральное число n
n = int(input("Введите натуральное число n: "))
#Инициализируем переменную для суммы
sum_result = 0
current_sin_sum = 0
#Вычисляем сумму 1/sin(1) + 1/(sin(1+2)) + ... + 1/(sin(1+2+...+n))
for i in range(1, n + 1):
    current_sin_sum += math.sin(i)
    sum_result += 1 / current_sin_sum
#Выводим результат
print(' сумма равна: ', sum_result)

2)
#Вводим действительное число x и натуральное число n
x = float(input("Введите действительное число x: "))
n = int(input("Введите натуральное число n: "))
#Инициализируем результат
result = 1
#Вычисляем выражение x(x-n)(x-2n)(x-3n)...(x-n²)
for i in range(n + 1):
    result *= (x - i * n)
#Выводим результат
print('Результат выражения x(x-n)(x-2n)(x-3n)...(x-n²) равен:', result)


3)
#Вводим действительное число x и натуральное число n
x = float(input("Введите действительное число x: "))
n = int(input("Введите натуральное число n: "))
#Инициализируем результат
result = 0
c = 1
#Вычисляем выражение 1/x + 1/(x(x+1)) + ... + 1/(x(x+1)...(x+n))
for i in range(n + 1):
    c *= (x + i)
    result += 1 / c
#Выводим результат
print('Результат выражения 1/x + 1/(x(x+1)) + ... + 1/(x(x+1)...(x+n)) равен: ', result)


4)
import math

#Вводим действительное число x и натуральное число n
x = float(input("Введите действительное число x: "))
n = int(input("Введите натуральное число n: "))
#Инициализируем результат
result = 0
#Вычисляем сумму x^i / i! для i от 1 до n
for i in range(1, n + 1):
    result += (x ** i) / math.factorial(i)
#Выводим результат
print('Результат выражения x^1/1! + x^2/2! + ... + x^n/n! равен:', result)





